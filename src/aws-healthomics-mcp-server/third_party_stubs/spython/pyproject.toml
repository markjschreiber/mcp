[project]
name = "spython"
version = "0.3.0+aws-healthomics-stub"
description = "Local compatibility stub satisfying cwltool's spython import without installing the real MPL-2.0 package"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["spython"]
